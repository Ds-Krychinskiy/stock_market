import { motion } from "framer-motion";
import styled from "styled-components";

export const PortfolioStyle = styled.div`
  margin-top: 2em;
  display: grid;
  justify-content: center;
  align-items: center;
`;

export const ChartWrapp = styled(motion.div)`
  display: flex;
  justify-content: center;
  width: 100%;
`;

